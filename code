n,x=map(int,input().split())

arr=[]
for i in range(x):
    arr.append(map(float,input().split()))   #for loop used to include decimal scores along with int scores for ex:90.5, 56.5,77 .... by appending in a empty list

for j in zip(*arr):
    print(sum(j)/len(j))  #average
